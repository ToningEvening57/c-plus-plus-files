#include <iostream>
using namespace std;

int main() {
	int x, y, z, a, b , c; // Declared but not initialized
	cout << "Value of x: " << x << endl;
	cout << y << "--" << z << "--" << a << "--" << b << "--" << c << "--" << endl;
	return 0;
}
